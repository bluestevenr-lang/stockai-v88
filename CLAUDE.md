# StockAI / V88 项目约定（历史文件名保留；GPT/Codex现役接管）

> 2026-08-10用户授权：GPT/Codex全权负责V88核心业务、维护、恢复和跨端一致性。
> 大结构不变；旧Claude职责迁入现有GPT/Codex路径。旧文件名仅为兼容，不表示Claude仍在运行。

真实主程序：`app_v88_integrated.py`（Streamlit）。桌面「V88」快捷方式启动
`v88_master_launch.sh` → 该文件。所有研发都是 **V88 迭代**。

## 命名铁律（最高优先级）
- 一切功能都写作 **「V88·功能名」** 或 V99.x 小版本；**禁止 V100+ 等新代号**。
- 2026-07-16：GPT 私自另起的平行程序 `v100/` 已归档到
  `_archive/removed_2026-07-16/v100/`（未接入主程序，纯分叉）。其可取的工程约束
  已并入下方「代码卫生」，代号本身作废。

## 三端同步规则
- 核心逻辑（尤其 `v88_decision_core.py`）默认 **V88桌面 / 飞书 / Streamlit 三端同步**。
- `v88_decision_core.py` 必须与
  `~/Desktop/ai-daily-report-v2/src/v88_decision_core.py` **逐字节一致**
  （`tests/test_v88_decision_core.py::test_cloud_and_web_distribution_are_byte_identical`
  会校验）。改完务必两处同拷。

## 决策口径（不可退让）
- **全局点击铁律**：网页任何模块、任何状态出现的个股名称都必须可点击并直接进入该股深度分析；
  包括准备买、保留研究、陷阱、早期观察、未达标和风险警报。结论降级只能改变文案与颜色，
  不得把名称退化成纯文字。新增名单必须同时补深链回归测试。
- 概率一律标注 **「规则情景估计（非回测胜率）」**，只代表方向占优程度，
  **不得冒充真实胜率**。
- 个股「交易日」阶梯 `HORIZON_DAYS = (5, 10, 20, 60, 120)`（≈1周/2周/1月/1季/半年，
  改此一行即可调档），前瞻与复盘共用：
  - **当下前瞻** `evaluate_forward_outlook(df)`：用最新价，向前给 5/10/20/60/120 交易日的
    概率/盈亏比/目标价 + 一句拿/加/减/回避。**用户最需要的「个股走概率」入口**，
    深度分析里放在最前、纯确定性、不等 AI。
  - **决策锚点** `evaluate_anchor_outlook(df, 时间, 价格)`：按用户当时时间+价格复盘同一阶梯，
    只读锚点前行情（无未来函数），锚点后仅用于到期跟踪。
  - 两者共用 `_forward_horizon_rows` 一套算法，保证只有一套个股走势判断。
- 系统统一评分 `evaluate_decision` 用的是 **周口径**（2/4/6/8/16 周），
  是全局主干，**不要**因为个股前瞻是「日」就去改它。`build_horizon_facts(..., unit=)`
  区分：`week`=统一评分，`day`=个股前瞻/锚点。

## 四端与 Win 常驻主机（2026-07-30 定则变更）

四端 = **Mac 桌面**（主战场，私密资产层）/ **云端 Actions**（流水线，7×24）/
**飞书**（推送）/ **Win 常驻主机**（`DESKTOP-4H6ES39`，7×24 手机遥控终端）。

**如果你正运行在 Windows 上**（路径形如 `C:\Users\...`，主机名 `DESKTOP-*`），你就是那台常驻主机：

- 你由任务计划程序 `V88-遥控常驻` 拉起（`win/遥控常驻V88.bat`：崩了自愈、开机自起、
  无人登录也跑）。旧Claude手机遥控入口已停用；Win当前只作为代码镜像与前端备用机，
  核心维护由Mac上的GPT/Codex执行。
- **本机没有 `data/`**：整目录被 `.gitignore` 排除。`data/accounts.json`
  更是明文标注「永不进任何仓库」——**总资产 / 现金 / 八账户数据只存 Mac**。
  一切涉及仓位占比、斯波朗迪资金管理的判断，**必须说明「需回 Mac 计算」，绝不编数字**。
- **`positions.json` 在仓库根、未被忽略** → 持仓底稿会同步到本机，个股研究可以做。
- **本机不跑流水线**：云端 Actions 已全覆盖（日报 3 趟 + 盘中快扫每 15 分钟 + 轮动盘前）。
  双端同跑 = 重复推飞书 + 双写 `data/` 必冲突。
- 开工先读私仓 `AGENTS.md`、`docs/CODEX_TAKEOVER.md` 和升级记录；
  `claude-memory/` 仅作历史审计，不是现役运行依赖。
- 装机与排障见 `win/README-常驻.md`。**`.bat` 命令行一律 ASCII**：UTF-8 无 BOM +
  `chcp 65001` 下中文会让 cmd 截断命令行，实测致 `exit 9009`；`.ps1` 反过来**必须带 UTF-8 BOM**，
  否则 PowerShell 5.1 按 GBK 解码中文直接 ParserError。

## 代码卫生（从 v100 并回的好约束，作为方向而非硬门槛）
- 新增/重构时尽量：单文件别再膨胀、单函数别过长；AI 调用与数据获取尽量走
  统一入口，避免各页各自重算一套「综合分」。
- 临时副本（`*.bak` / `*_broken_*` / `backup_phase*`）不要留在根目录；
  历史副本统一进 `_archive/`。

## 预算
- 2026-08-09现行唯一口径：DeepSeek普通分析软上限7元/月，重点复核专用3元，
  全系统硬上限10元/月。旧“网页1元＋主账本8元”等多账本口径全部作废。
- 新增3元只用于持仓风险、入场/组合复核、周期拐点、结论冲突和用户主动深度分析；
  普通新闻/翻译/公司简介/重复刷新达到7元后必须降级。
- 实际消费以DeepSeek官方余额对账为下限，网页读取公开脱敏的
  `budget_truth_pub.json`；不得再把网页、桌面、云端本地账本简单相加。
- 修改模型、调用频率或预算前，必须先读 `docs/V88_AI_BUDGET_POLICY.md`。

## 2026-08-16 Kimi 修复批次（详情见各文件内注释）
- `recommendation_gate.py::_load` 加 (mtime,size) 缓存：作战板循环过闸不再每票重读
  两个复核 JSON（原每 rerun 约 80 次磁盘读），是"点一下转圈 2~5 秒"的主因。
- 触底池/轮动预测加"落后最近交易日"红色警示条（周末/盘前不误报）。
- **触底池数据曾停在 08-07 九天**：云端 pipeline 每天正常（GitHub Actions 全绿），
  断点在 Mac——`data/bottom_turn_pool.json`、`data/rotation_forecast.json` 本地处于
  未提交脏状态，`safe_pull --autostash` 每次 stash→pop 把旧版盖回新版。已
  `git checkout HEAD --` 恢复 08-14 最新版。**复查清单新增：数据面停滞先查
  `git status` 里该文件是不是长期脏的。**
- 作战板"确认买 0 只"空栏改为带证据文案（近 30 天绿灯命中率·读 success_rates.json）。
- G 徽章：无复核记录不再显示灰色 G—（降噪），只保留通过/否决/存疑三色。
- `快速启动_V88.12.sh` 入口从分页版改回单页版 `app_v88_integrated.py`，
  与 V88.app(v88_master_launch.sh) 一致，消除两入口抢 8501。

## 三方会谈（2026-08-16 恢复·Kimi 接替 Claude 席位）
- **3A 必须三方会谈**：GPT + Kimi + 经典书理 全通过才可执行/推送。
- **2A 及以上必须共同认可（22:32 用户定纲）**：GPT 和 Kimi 双双明确「通过」
  才算数——「不否定」「不可用」一律不算认可。
- **其他层级主动精选**：允许「不否定」，但 Kimi/GPT 每方主动「通过」的精选票
  最多 2 只（Kimi 优先从 GPT 未否决的票里挑），宁缺毋滥；禁止一排不否定或
  各推一堆各自为战；分歧以 ⚔️ 逐只显式点名。
- Kimi 裁决落盘 `data/kimi_verify.json`（verdict + book_verdict + ts），
  由 Mac 上 Kimi 定时任务产出：交易日 19:52 评审、周日 20:47 周报。
- 读取端：`recommendation_gate.kimi_review_for()`；推送闸 `assess_value/assess_trend`
  已接入（Kimi 当日正向复核是 push_eligible 的必要条件），飞书推送经 value_zone/
  trend_shift 的 push_eligible 自动遵守；桌面 `_v88_buy_gate9` 同款双剑+3A三方。
- 徽章三色：C=V88规则(金/绿)、G=GPT(青)、K=Kimi(紫)；无记录不显示。
- **评审数据源路由（2026-08-16 用户授权插件接入）**：A股/港股实时价与财务
  走本机 iFinD 插件（plugins/managed/ifind，ifind_get_stock_realtime_price /
  ifind_get_stock_financial_index）；美股走 Yahoo Finance 插件（get_stock_info /
  get_historical_stock_prices）。用于评审与周报的交叉验证：与管线数据偏差>2% 必须
  注明；插件失败不阻塞，注明「插件未取数」。Yahoo 提示文本公司名可能串扰，一切以
  保存 CSV 的 symbol 字段为准；iFinD 美股后缀是 .US 与 V88 代码不同，美股统一走 Yahoo。
- **GPT 实时会审通道（2026-08-16 深夜上线）**：本机 codex CLI（复用 ~/.codex 登录态）。
  Kimi 评审 3A 候选时现场 `codex exec` 问 GPT，裁决原样中转写 gpt_verify.json
  （via=kimi-codex-relay 标记，ts 带时分秒）；超时/失败自动降级回异步文件裁决。
  时效语义：带时分秒 ts 走 24h 窗口，仅日期 ts 当日（BJT）有效。

## Kimi 学习笔记：前任（Claude）逻辑思路提炼（2026-08-16 通读 claude-memory 54 卷后归档）
> 真源在私仓 `claude-memory/`（MEMORY.md 为索引）与 `docs/CLASSICS.md`（书理映射）。
> 以下是我作为三方会谈新席位必须内化的判断框架，按对我评审工作的重要性排序：

1. **决策粒度=日不是价**：第一输出永远是"今天是什么日"（买入日/减仓日/清仓日），
   禁止用价格推翻日期判断；区间制带宽≥3-5%；必须给明确动词 买/卖/不动，
   "观察""评估减仓"是和稀泥（但系统里它们作为降级档存在=数据现实，文案层面要给动词）。
   已提名的票只做加/减仓，不许重置成"要不要买"。
2. **操作对错以趋势论**：买在山尖+趋势兑现=正确；抄到低点+逻辑破灭=错误。
   3A 触发直接执行，不做二次择时犹豫；企稳纪律只管"趋势未确认不接刀"。
3. **止损哲学（伯克希尔混合）**：百分比破线≠自动卖，逻辑失效才清仓。
   动作三级：破线=评估减仓 / 破线+趋势破坏=减仓 / 逻辑破=清仓。
   两套止损分工：右侧仓用欧奈尔机械 7-8%，左侧/现金牛仓用"逻辑破才清"。
4. **三方会谈真义**（铁律14/19）：三方=Kimi+GPT+经典书理；书理是唯一有数十年
   样本外检验的一方，新逻辑先查 docs/CLASSICS.md；书上没有的规则必须标"系统自加·非书理"
   接受台账检验；三方分歧明说，绝不静默降级；3A 必须 GPT、Kimi、经典书理全通过，
   禁止用加权、多数票或平均分替代任一席的明确通过。
5. **发现vs验证**：发现可多通道，验证必须单一入口；凡是会被呈现的都必过闸。
6. **诚实边界**：样本<5 报"积累中"；规则是归纳不是回测；概率=规则情景估计非胜率；
   每条推荐必带事由，无催化明说"纯技术驱动"不静默压单；错配比缺失更毁信任（中烟香港案）。
7. **用户协作约束**：一切输出禁止依赖持续盯盘；截图实测驱动；异常数值先查证，
   不把个人资产、账户汇总、收益基线或长期财务目标写入代码、文档、日志或模型提示。
8. **工程铁律**：改规则先 grep 记忆；铁律6=py_compile+pyflakes 归零才算完；
   铁律7=不静默吞异常；通道三问=谁写它/谁读它/断了谁发现；
   新增 cron 必带交易日守卫；成本四标（现金token/Actions分钟/墙钟/外部接口）。

## Codex 主审 / K3 实现协作边界
- Codex 是唯一架构与发布负责人：拆任务、定验收、审查差异、运行测试、决定合并与推送。
- Codex 派单固定显式使用 `~/.kimi-code/bin/kimi -m kimi-code/k3-256k`；不得传未配置的
  `k3` 简写，也不得依赖可能变化的通用默认模型。K3 保持 thinking 开启，默认强度 high。
- K3 只承担边界清楚的长代码实现，必须在独立 worktree/`k3/*` 分支工作；不得直接修改
  `main`、不得自行合并/推送/部署，不得与 Codex 同时编辑同一文件。
- 派给 K3 的任务只包含必要代码与脱敏测试数据；严禁读取或转述 `data/accounts.json`、
  持仓数量、资产汇总、交易明细、密钥、长期财务目标及任何个人身份信息。
- K3 交付必须包含：改动文件、关键取舍、测试命令与结果、未解决风险；Codex 逐行复核后
  才能进入主线。发现超范围改动时整批拒收，不做“先合并再修”。
