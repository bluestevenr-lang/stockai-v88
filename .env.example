# Optional V88 configuration; credentials and user settings stay local.
# Free market sources and the existing Codex subscription are used.
V88_GPT_MODEL=gpt-6-astra
# V88_DISABLE_LLM=1
# Keep any existing local proxy configuration; no paid API key is required.
