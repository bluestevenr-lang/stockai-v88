# 建议稿：V88 引擎级 AI 预算闸（每月现金上限 10 元）

> 铁律23 流程件：本稿仅为建议，不改动私仓 src/。请 Mac 端过 GPT 审查闸后以仓库形式下发。

## 背景

2026-08-24 用户发现 Moonshot 现金余额从 65 元降至 35.41 元（约 29.6 元去向需核），定纲：**每月现金上限 10 元**。

Win 侧已落地（本日）：余额快照台账 `~/.openclaw/moonshot_balance.jsonl`、预算哨兵脚本 `win/k3_budget.py`、体检脚本超支判级（>8 ⚠️ / >10 ❌）、每日 21:07 预算哨兵 cron、AGENTS.md 预算红线规则。

## 请 Mac/引擎侧补齐的三件事

1. **`v88_ai_budget.py` 加硬闸**：月度现金估算超过 10 元时，按量调用一律降级为确定性底稿（现有 fail-closed 路径），并在 `data/ai_budget.json` 写明 `cap_exceeded: true` 与当月估算值。估算口径建议与 Win 台账一致：余额快照差分（跳增=充值）。
2. **K2.7 替换 DeepSeek 迁移**（用户 08-20 定纲，当时确认可行）落地时，把单价写进预算表：K2.7-code 输入 6.5 / 输出 27 元每 M tokens，Batch 价 3.9/16.2；初筛批量任务优先走 Batch（6 折）。
3. **消耗源核查**：65→35.41 的约 29.6 元中，Win 侧可考的只有 k3ask 按量 4.38 元（17 次，08-20）+ 网关 K2.7 接待员会话与三方会审 cron（数次，估计个位数元）。其余约 20 元疑似 Mac 端按量消耗（如 kimi 审核 2453 只个股若走过按量接口，单次全量即可达十元级）。请在 Mac 侧核查 08-17～08-24 的按量调用记录，或在 platform.moonshot.cn 控制台导出账单明细核对。

## 验收

- 私仓 `data/ai_budget.json` 出现 `cap_exceeded` 字段与月度估算值；
- 全月任何一天 Moonshot 余额下降累计超过 10 元时，引擎自动停按量、Win 哨兵同步 ❌ 报警。
