# ── Secrets（绝对不能上传）──────────────────────────────
.streamlit/secrets.toml

# ── Python 缓存 ──────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.Python

# ── 本地扫描/简报缓存（运行时生成，不需要上传）──────────
.cache_brief/
.cache_stock_data/
scan_cache/
scan_results.json
*.pkl

# ── 备份文件（保留在本地即可）──────────────────────────
app_backup_*.py
app_v88_broken*.py
app_v88_integrated_backup*.py
app_v88_integrated_broken*.py
backup_phase*.py
backup_v88_*/
auto_reporter_v*.py
auto_reporter_final.py
auto_report_v88_optimized.py

# ── 测试文件 ─────────────────────────────────────────────
test_*.py
quick_test_*.py
run_test.sh
FINAL_TEST.md

# ── 旧版文件 ─────────────────────────────────────────────
app.py
app_backup_v72.py
aihub26/
aihub26_deploy/
ai/
core/
docs/
providers/
qlib_system/
ui/

# ── agent 记忆文件 ───────────────────────────────────────
agent_memory_*.json

# ── Excel / 截图 / 日志 ──────────────────────────────────
*.xlsx
~$*.xlsx
*.png
*.log

# ── Win 常驻主机运行时产物（整目录排除，安全命门）─────────
#  *.log 只挡住日志本身，挡不住 quarantine 里的备份：
#  win\logs\quarantine\credentials.*.json.bak 是【真实 accessToken/refreshToken】，
#  而本仓库是公开仓 —— 任何人一次 git add -A 就会把令牌推上公网。
#  故整目录排除（2026-07-30 实测 git add -An 确认会 stage 该 .bak）。
win/logs/
win/feishu_raw_log.jsonl
win/.monitor_last_pos

# ── macOS ────────────────────────────────────────────────
.DS_Store

# ── 环境 ─────────────────────────────────────────────────
.env
venv/
.venv/

# ── 个人数据（公开仓绝不上传）────────────────────────────
watchlist.json
config.toml
positions.json
*.plist
CLAUDE.md
HANDOFF.md
